name: CI

on:
  push:
  pull_request:

permissions:
  contents: read

env:
  CARGO_AUDIT_VERSION: "0.22.1"
  NEOVIM_SHA256: "95aaa8e89473f5421114f2787c13ae0ec6e11ebbd1a13a1bd6fcf63420f8073f"
  NEOVIM_VERSION: "0.10.4"
  SELENE_VERSION: "0.31.0"
  SHELLCHECK_SHA256: "6c881ab0698e4e6ea235245f22832860544f17ba386442fe7e9d629f8cbedf87"
  SHELLCHECK_VERSION: "0.10.0"
  STYLUA_VERSION: "2.5.2"
  TOGGLETERM_COMMIT: "50ea089fc548917cc3cc16b46a8211833b9e3c7c"

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4.3.1
        with:
          persist-credentials: false
      - name: Check out ToggleTerm v2.13.1 compatibility fixture
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4.3.1
        with:
          repository: akinsho/toggleterm.nvim
          ref: ${{ env.TOGGLETERM_COMMIT }}
          path: .ci/toggleterm.nvim
          persist-credentials: false
      - uses: dtolnay/rust-toolchain@fa04a1451ff1842e2626ccb99004d0195b455a88 # master, 2026-06-30
        with:
          toolchain: "1.95.0"
          components: rustfmt, clippy
      - uses: Swatinem/rust-cache@c19371144df3bb44fab255c43d04cbc2ab54d1c4 # v2.9.1
      - name: Install Neovim and Lua
        run: |
          sudo apt-get update
          sudo apt-get install -y lua5.1
          curl --fail --silent --show-error --location \
            --proto '=https' --proto-redir '=https' --tlsv1.2 \
            --retry 5 --retry-all-errors \
            --output /tmp/nvim.tar.gz \
            "https://github.com/neovim/neovim/releases/download/v${NEOVIM_VERSION}/nvim-linux-x86_64.tar.gz"
          printf '%s  %s\n' "${NEOVIM_SHA256}" /tmp/nvim.tar.gz | sha256sum --check --strict
          sudo tar -C /opt -xzf /tmp/nvim.tar.gz
          printf '%s\n' /opt/nvim-linux-x86_64/bin >> "${GITHUB_PATH}"
          /opt/nvim-linux-x86_64/bin/nvim --version
      - name: Install ShellCheck
        run: |
          curl --fail --silent --show-error --location \
            --proto '=https' --proto-redir '=https' --tlsv1.2 \
            --retry 5 --retry-all-errors \
            --output /tmp/shellcheck.tar.xz \
            "https://github.com/koalaman/shellcheck/releases/download/v${SHELLCHECK_VERSION}/shellcheck-v${SHELLCHECK_VERSION}.linux.x86_64.tar.xz"
          printf '%s  %s\n' "${SHELLCHECK_SHA256}" /tmp/shellcheck.tar.xz | sha256sum --check --strict
          tar -C /tmp -xf /tmp/shellcheck.tar.xz
          sudo install -m 0755 "/tmp/shellcheck-v${SHELLCHECK_VERSION}/shellcheck" /usr/local/bin/shellcheck
          shellcheck --version
      - name: Install Lua lint tools
        run: |
          cargo install stylua --version "${STYLUA_VERSION}" --locked
          cargo install selene --version "${SELENE_VERSION}" --locked
      - name: Check Neovim version
        run: nvim --headless --clean +'lua local v = vim.version(); if v.major == 0 and v.minor < 10 then vim.api.nvim_err_writeln(string.format("nvim-remote-mirror requires Neovim 0.10+, got %d.%d.%d", v.major, v.minor, v.patch or 0)); vim.cmd("cquit 1") end; if vim.fn.exists("*readblob") ~= 1 then vim.api.nvim_err_writeln("nvim-remote-mirror requires readblob()"); vim.cmd("cquit 1") end' +qa
      - name: Rust format
        run: cargo fmt --all --check
      - name: Rust clippy
        run: cargo clippy --workspace --all-targets --locked -- -D warnings -D clippy::undocumented_unsafe_blocks
      - name: Rust tests
        run: cargo test --workspace --locked
      - name: Lua syntax
        run: git ls-files -z '*.lua' | xargs -0 luac5.1 -p
      - name: Lua format
        run: stylua --check lua plugin tests
      - name: Lua lint
        run: selene lua plugin tests
      - name: Headless Neovim tests
        run: find tests -maxdepth 1 -name '*.lua' -print0 | sort -z | while IFS= read -r -d '' test; do nvim --headless -u NONE -l "$test"; done
      - name: ToggleTerm v2.13.1 integration smoke
        env:
          NRM_TOGGLETERM_DIR: ${{ github.workspace }}/.ci/toggleterm.nvim
        run: |
          test "$(git -C "${NRM_TOGGLETERM_DIR}" rev-parse HEAD)" = "${TOGGLETERM_COMMIT}"
          nvim --headless -u NONE -l tests/toggleterm_upstream_smoke.lua
      - name: Shell syntax
        run: bash -n scripts/*.sh
      - name: Shell lint
        run: shellcheck scripts/*.sh
      - name: Dependency audit
        run: |
          cargo install cargo-audit --version "${CARGO_AUDIT_VERSION}" --locked
          cargo audit -f Cargo.lock -D warnings
      - name: Whitespace
        run: git diff --check
      - name: Perf smoke
        run: scripts/perf_smoke.sh --small
